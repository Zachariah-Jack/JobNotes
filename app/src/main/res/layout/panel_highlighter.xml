<?xml version="1.0" encoding="utf-8"?>
<LinearLayout
    xmlns:android="http://schemas.android.com/apk/res/android"
    xmlns:app="http://schemas.android.com/apk/res-auto"
    android:layout_width="match_parent"
    android:layout_height="wrap_content"
    android:orientation="vertical">

    <!-- Icon-only toggle group: Freeform / Straight -->
    <com.google.android.material.button.MaterialButtonToggleGroup
        android:id="@+id/highlightModeGroup"
        android:layout_width="match_parent"
        android:layout_height="wrap_content"
        android:gravity="center_horizontal"
        app:singleSelection="true"
        app:selectionRequired="true">

        <com.google.android.material.button.MaterialButton
            android:id="@+id/btnHlFreeform"
            style="@style/Widget.Material3.Button.OutlinedButton"
            android:layout_width="wrap_content"
            android:layout_height="wrap_content"
            android:checked="true"
            app:icon="@drawable/ic_highlight_freeform"
            app:iconTint="@android:color/black"
            app:iconSize="24dp"
            app:iconPadding="0dp"
            android:contentDescription="Freeform" />

        <com.google.android.material.button.MaterialButton
            android:id="@+id/btnHlStraight"
            style="@style/Widget.Material3.Button.OutlinedButton"
            android:layout_width="wrap_content"
            android:layout_height="wrap_content"
            app:icon="@drawable/ic_highlight_straight"
            app:iconTint="@android:color/black"
            app:iconSize="24dp"
            app:iconPadding="0dp"
            android:contentDescription="Straight" />
    </com.google.android.material.button.MaterialButtonToggleGroup>

    <com.google.android.material.slider.Slider
        android:id="@+id/sliderHighlightSize"
        android:layout_width="match_parent"
        android:layout_height="wrap_content"
        android:value="24"
        android:valueFrom="8"
        android:valueTo="100"
        android:stepSize="1" />

    <com.google.android.material.slider.Slider
        android:id="@+id/sliderHighlightOpacity"
        android:layout_width="match_parent"
        android:layout_height="wrap_content"
        android:value="50"
        android:valueFrom="0"
        android:valueTo="100"
        android:stepSize="1" />
</LinearLayout>
