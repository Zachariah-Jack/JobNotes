<resources xmlns:tools="http://schemas.android.com/tools">
    <!-- Base application theme (light / default). -->
    <style name="Base.Theme.JobNotes" parent="Theme.Material3.DayNight.NoActionBar">
        <!-- Solid opaque status bar with dark icons -->
        <item name="android:statusBarColor">@android:color/white</item>
        <item name="android:windowLightStatusBar">true</item>

        <!-- Solid opaque navigation bar with dark icons -->
        <item name="android:navigationBarColor">@android:color/white</item>
        <!-- NOTE: can't use android:windowLightNavigationBar (API 27+) since your minSdk=26 -->
    </style>

    <!-- Alias theme actually used by your activities -->
    <style name="Theme.JobNotes" parent="Base.Theme.JobNotes" />
</resources>
